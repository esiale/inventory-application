<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/head'); %>
  </head>
  <body>
    <div class="wrapper">
      <%- include('./partials/header'); %>
      <main>
        <%- include('./partials/nav'); %>
        <div class="content">
          <h1>Delete format:</h1>
          <h2><%=format.name %></h2>
          <% if (products.length) { %>
          <div class="delete-item">
            This format has the following associated products:
            <ul>
              <% products.forEach((product) => { %>
              <li>
                <a href="<%=product.url%>"
                  ><%= product.album.name %> - <%= product._id %></a
                >
              </li>
              <%})%>
            </ul>
            <span class="warning"
              >You have to delete all products before proceeding.</span
            >
          </div>
          <%} else {%>
          <form method="POST" action="">
            <span class="warning"
              >This action can't be reversed! Are you sure?</span
            >
            <button type="submit">Delete</button>
          </form>
          <%}%>
        </div>
      </main>
      <%- include('./partials/footer'); %>
    </div>
  </body>
</html>
