<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/head'); %>
  </head>
  <body>
    <div class="wrapper">
      <%- include('./partials/header'); %>
      <main>
        <%- include('./partials/nav'); %>
        <div class="content">
          <h1>Viewing genre's details</h1>
          <h2><%= genre_detail.name %></h2>
          <div class="detail-container">
            <div class="detail-text">
              <h3>Albums:</h3>
              <ul>
                <% albums.forEach((album) => { %>
                <li><a href="<%= album.url %>"><%= album.name %></a></li>
                <%})%>
              </ul>
            </div>
          </div>
          <div class="detail-list">
            <h2>Listing products:</h2>
            <% if (!products.length) { %> Currently there's no products for this
            genre. <%} else { %>
            <ul>
              <% products.forEach((product) => { %>
              <li>
                <a href="<%= product.url %>"
                  ><%= product.product_info.format.name %> - <%= product.name %>
                  - <%= product.product_info.artist.name %> -
                  <%=product.product_info._id%></a
                >
              </li>
              <%})%>
            </ul>
            <%}%>
          </div>
          <div class="button-container">
            <a href="<%=genre_detail.url%>/delete"><button>Delete</button></a>
            <a href="<%=genre_detail.url%>/update"><button>Update</button></a>
          </div>
        </div>
      </main>
      <%- include('./partials/footer'); %>
    </div>
  </body>
</html>
